### demo 2: setup environment for nested hosts

demo file

install some lxc machines as: db, web
setup ssh server
add hosts: db, web into /etc/ansible/host
run script to add authorized_key
demo in virtual box : ok
